package com.example.kotlin.chap03.ex03

// when
/*
fun main() {
  var data = 10

  when (data) {
    10 -> println("data is 10")
    20 -> println("data is 20")
    else -> println("data is not valid data")
  } // data is 10
}

// 문자열 타입 조건 사용
fun main() {
  var data = "hello"

  when (data) {
    "hello" -> println("data is hello")
    "world" -> println("data is world")
    else -> println("data is not valid data")
  } // data is hello
}

// 다양한 유형의 조건 제시
fun main() {
  var data: Any = 10

  when (data) {
    is String -> println("data is String")           // 타입 검사
    20, 30 -> println("data is 20 or 30")            // 여러 값 비교
    in 1..10 -> println("data is 1..10")             // 범위 검사
    else -> println("data is not valid")
  } // data is 1..10
}

// when 조건문을 if 처럼 사용
fun main() {
  var data = 10

  // when을 표현식으로 사용하여 result에 대입
  val result = when {
    data <= 0 -> "data is <= 0"
    data > 100 -> "data is > 100"
    else -> "data is valid"
  }

  println(result) //data is valid
}
*/

